# 需求澄清（消解矛盾）— vidforge

我在你的答案里发现了**范围上的矛盾**，需要你定夺后才能生成正式需求文档。请填 `[Answer]:`，完成后告诉我 "完成 / done"。

---

## 矛盾说明
你在 **Q11** 表示："开源工具，第一版可以不用特别完整，**先实现基本功能**"。
但同时：
- **Q4 = E**：四种能力全做（t2v / i2v / r2v / video-edit）
- **Q8 = A**：多任务并发队列管理
- **Q14 = A**：全量强制属性测试（PBT）

"先做基本功能"与"四种能力 + 并发队列 + 全量 PBT"在工作量上互相打架。下面 3 个问题用来确定第一版的真实边界。

---

## Clarification 1：第一版到底做几种能力？
（四种能力共用同一个 API 端点，差别只在 model 字段和输入素材。但 i2v/r2v/video-edit 各自需要不同的素材上传 UI——图片、多图、视频，这些 UI 才是真正的工作量。）

A) **第一版只做 t2v + i2v**（文生视频 + 图生视频），r2v 和 video-edit 放到第二版（最符合"先实现基本功能"— 推荐）

B) 第一版只做 t2v（最小可用，先把异步任务全链路跑通）

C) 四种全做（维持 Q4=E，接受更大的第一版工作量）

X) Other（请描述）

[Answer]: C

## Clarification 2：第一版任务管理做到什么程度？
A) **先做串行单任务**（提交一个、轮询、下载，跑通全链路），并发队列放第二版（最符合"先实现基本功能"— 推荐）

B) 第一版就要并发队列 + 进度面板（维持 Q8=A，工作量更大）

X) Other（请描述）

[Answer]:B

## Clarification 3：第一版的测试策略？
（PBT = 属性测试，对"输入参数构造、校验、序列化"这类纯逻辑很有价值，但全量强制会显著拖慢第一版出活。）

A) **第一版只对纯逻辑做 PBT**（参数校验、请求体构造、状态机流转），UI 和网络层用普通单测/集成测试（务实 — 推荐，相当于把 Q14 调成 Partial）

B) 维持 Q14=A，全量强制 PBT（更严格，更慢）

C) 第一版先用普通单测，PBT 以后再补

X) Other（请描述）

[Answer]:A
